name: DepremDudugu
description: Yardım için düdük çalar.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.2.1+4

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  audioplayers: ^0.16.2
  cupertino_icons: ^1.0.0
  flutter_launcher_icons: ^0.8.1
  path_provider: ^1.6.24

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icons/appicon.png"

flutter:
  uses-material-design: true
  assets:
    - assets/audios/
    - assets/icons/
    - assets/img/
    - assets/lang/en.json
    - assets/lang/tr.json
